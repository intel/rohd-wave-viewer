name: module_structure_api
description: Data Layer for waveform viewer.
version: 0.0.1

publish_to: none

environment:
  sdk: ^3.0.0

# Add regular dependencies here.
dependencies:
  equatable: ^2.0.5

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
